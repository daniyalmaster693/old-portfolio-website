<div class="navbarholder">
  <div class="navcontentholder">
    <div class="left-side">
      <a class="navlink-home" href="/" aria-current="page"
        ><p>Daniyal Master</p></a
      >
    </div>

    <div class="right-side">
      <a class="navlink" href="/projects" aria-current="page"><p>Projects</p></a
      >
      <a class="navlink" href="/blogs" aria-current="page"><p>Blogs</p></a>
    </div>
  </div>
</div>

<style>
  .navbarholder {
    z-index: 999;
    display: flex;
    justify-content: center;
    align-items: center;
    position: sticky;
    top: 0;
    width: 100%;
  }

  .navcontentholder {
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 55px;
    width: 100%;
    max-width: 600px;
    border-radius: 999px;
    margin: 20px 15px 0px 15px;
    padding: 0px 30px 0px 30px;
    border: 1px solid #3f3f48;
    box-shadow: var(--box-shadow);
    background: linear-gradient(137deg, #121114d9 4.87%, #0d0c0fcc 80%);
    backdrop-filter: blur(10px);
    --webkit-backdrop-filter: blur(10px);
    position: sticky;
    top: 0;
    z-index: 2;
  }

  .navlink-home,
  .navlink {
    font-size: 0.9rem;
    font-family: var(--Inter);
    font-weight: 400;
    letter-spacing: 0.023rem;
    line-height: 1.6em;
    text-decoration: none;
    transition: var(--ease-in-out);
  }

  .navlink-home {
    color: var(--primary-grey);
  }

  .navlink-home:hover {
    color: var(--purple);
  }

  .navlink {
    color: var(--secondary-grey);
  }

  .navlink:hover {
    color: var(--primary-grey);
  }

  .left-side {
    display: flex;
    align-items: center;
  }

  .right-side {
    display: flex;
    align-items: center;
    gap: 25px;
  }
</style>
